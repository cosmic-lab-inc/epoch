[package]
name = "warden"
version.workspace = true
edition.workspace = true
license.workspace = true
description.workspace = true
authors.workspace = true
categories.workspace = true
keywords.workspace = true

[dependencies]
argon2 = { version = "0.5.3", features = ["alloc", "password-hash", "rand", "std"] }
anyhow = { workspace = true }
log = { workspace = true }
solana-sdk = { workspace = true }
redis = "0.25.2"
thiserror = { workspace = true }
tokio = { workspace = true }
uuid = { version = "1.3.0", features = [
    "v4", # Lets you generate random UUIDs
    "fast-rng", # Use a faster (but still sufficiently random) RNG
    "macro-diagnostics", # Enable better diagnostics for compile-time UUIDs
] }
#common_utils = { git = "https://github.com/staratlasmeta/common_utils.git", branch = "token_2022" }
#player_profile = { git = "https://github.com/cosmic-lab-inc/player_profile.git", branch = "main", features = ["client"] }
#profile_vault = { git = "https://github.com/cosmic-lab-inc/player_profile.git", branch = "main", features = ["client"] }
#anchor-lang = { workspace = true }
#ahash = ">0.8.5,<=0.8.6"